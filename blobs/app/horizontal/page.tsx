/**
 * 水平佈局版本 (Horizontal Layout Version)
 *
 * 此頁面保存了水平使用的介面配置：
 * - 中央置中的「點 按 喚 醒」按鈕
 * - 左上角的 logo
 * - 結束對話按鈕位於 logo 下方
 * - 右上角的對話切換按鈕（SVG 圖標）
 * - 右下角的麥克風按鈕
 * - 銀白色 SVG 反射陰影
 * - 玻璃擬態設計按鈕樣式
 */

import SplineViewer from '../SplineViewer';

export default function HorizontalLayout() {
  return (
    <main>
      <SplineViewer />
    </main>
  );
}
